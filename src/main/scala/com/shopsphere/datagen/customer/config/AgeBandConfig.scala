package com.shopsphere.datagen.customer.config

case class AgeBandConfig(
                          minAge: Int,
                          maxAge: Int,
                          weight: Double
                        )